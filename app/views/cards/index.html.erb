<% javascript 'card_all.js' %>

<h1>My Cards</h1>

<div class="cards-all">
    <% @cards.each_with_index do |card, i| %>
        <% lastKlass = '' %>
        <% lastKlass = "last#{(@cards.count)%3}" if i == @cards.count - 1 %>
        <%= render partial: 'shared/card', locals: { card: card, lastClass: lastKlass} %>
        <!--%= link_to 'Edit', edit_card_path(card) %-->
        <!--%= link_to 'Destroy', card, method: :delete, data: { confirm: 'Are you sure?' } %--> 
    <% end %>
</div>
<a href=<%= new_card_path %>>
  <div class="glyphicon glyphicon-plus-sign medium-glyph"></div>
  New Card
</a>
