<%= form_for(@special_ability) do |f| %>
  <% if @special_ability.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@special_ability.errors.count, "error") %> prohibited this special_ability from being saved:</h2>

      <ul>
      <% @special_ability.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :rule %><br>
    <%= f.text_field :rule %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
